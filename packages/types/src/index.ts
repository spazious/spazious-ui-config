export enum Features {
  husky = "husky",
  prettier = "prettier",
  linter = "linter",
  staged = "staged",
  typescript = "typescript",
  storybook = "storybook",
  bundler = "bundler",
  testing = "testing",
  publisher = "publisher",
}

export enum Presets {
  default = "default",
  react = "react",
  "react-app" = "react-app",
}
